/*
===============================================================================
File        : 01_primary_indexes.sql
Description : Primary key indexes.
===============================================================================

PostgreSQL automatically creates a unique B-Tree index for every PRIMARY KEY.

No additional statements are required.
*/

SELECT 'Primary key indexes already exist.';